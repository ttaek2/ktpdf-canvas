// @flow
import * as React from 'react';
const styled = require('./common.css');

type Props = {
  
};
type State = {
  
};
export class Test extends React.Component<Props, State>{
  render() {
    return (
      <div className='testDivDiv'>
          test div div!
      </div>
    );
  };
};